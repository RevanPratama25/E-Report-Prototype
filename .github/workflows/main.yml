name: EReport

on:
  push:
    branches: [ main ]

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 151.243.222.70
          username: random@arcueia.my.id
          password: randompassword
          protocol: ftps
          security: explicit
          passive: true
          timeout: 60000
          exclude: |
            **/.git/**
            **/node_modules/**
